<h1>{{user.first_name}} {{user.last_name}} [{{user.id}}]</h1>
<ul class="list">
  <li class="list-item"><strong>Email: </strong>{{user.email}}</li>
  <li class="list-item"><strong>Telefone: </strong>{{user.phone}}</li>
</ul>

<a class="btn" href="/">Voltar</a>

<form method="POST" action="/user/delete/{{user.id}}?_method=DELETE">
  <input type="submit" class="btn btn-del" value="Deletar">
</form>
